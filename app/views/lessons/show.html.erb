<h1><%= @lesson.name%></h1>
<h4>In section: <a href="/chapters/<%= @lesson.section.chapter.slug %>/sections/<%= @lesson.section.slug %>"><%= @lesson.section.name %></a></h4>

<p><%= @lesson.content%></p>

<button><a href="/sections/<%= @lesson.section.slug%>/lessons/<%= @lesson.slug %>/edit">Edit Lesson</a></button>
<button><a href="/lessons/<%= @lesson.slug %>"
    data-confirm="Sure you want to delete this?"
    data-method="delete"
    rel="nofollow">Delete Lesson</a></button>

<% if @lesson.previous != nil %>
  <p><a href="/sections/<%= @lesson.section.slug%>/lessons/<%= @lesson.previous.slug %>">Previous Lesson</p>
<% end %>
<% if @lesson.next != nil %>
  <p><a href="/sections/<%= @lesson.section.slug%>/lessons/<%= @lesson.next.slug %>">Next Lesson</p>
<% end %>
<p><a href="/">Return to the Table of Contents</a></p>
